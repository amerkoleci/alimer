name: Build

on:
  push:
    branches:
      - 'main'
    paths-ignore:
      - 'docs/**'
      - '*.md'
      - .github/workflows/build_native.yml
      - 'src/native/**'
  pull_request:
    paths-ignore:
      - 'docs/**'
      - '*.md'
      - .github/workflows/build_native.yml
      - 'src/native/**'

jobs:
  build:
    runs-on: windows-2025-vs2026

    steps:
    - name: Checkout
      uses: actions/checkout@v7

    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v6
      with:
        global-json-file: ./global.json

    - name: Build/Pack
      run: dotnet pack Alimer.slnx --configuration Release -p:Packing=true
